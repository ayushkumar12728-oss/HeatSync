# =============================================================================  
# Landsat Historical LST Configuration
# =============================================================================
# These settings control the historical Landsat LST pipeline.
# They can be set via environment variables with the UDT_ prefix.

# --- Landsat pipeline settings ---
# landsat_enabled: bool = True
# landsat_max_cloud_cover: float = 30.0
# landsat_cache_dir: Path = data/processed/temporal
# landsat_start_date: str | None = None
# landsat_end_date: str | None = None
