# scripts/

Utility scripts and one-off tooling that do not belong to a specific engine.

- The **data pipeline** (download + process + feature engineering) lives in
  [`gis-engine/`](../gis-engine/).
- The **AI/ML pipeline** lives in [`ai-engine/`](../ai-engine/).
- The **simulation library** lives in [`scenario-engine/`](../scenario-engine/).
